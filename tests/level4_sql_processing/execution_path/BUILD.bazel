load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "execution_path_test",
    srcs = ["execution_path_test.cpp"],
    deps = [
        "@com_google_googletest//:gtest_main",
        "//src/core:core",
        "//src/logger:logger",
        "//src/exception:exception",
        "//src/sql_executor:sql_executor",
        "//src/error_handler:error_handler",
    ],
    copts = [
        "-std=c++20",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = ["-fprofile-instr-generate"],
    tags = ["coverage", "level4", "sql_processing", "path"],
)