load("@rules_cc//cc:defs.bzl", "cc_test")

# Level 4 SQL Processing Tests
# This level contains SQL processing and execution related tests, organized by functionality

# Execution context tests
cc_test(
    name = "execution_context_tests",
    srcs = glob([
        "execution_context/*.cpp",
    ], allow_empty = True),
    deps = [
        "@com_google_googletest//:gtest_main",
        "//src/core:core",
        "//src/logger:logger",
        "//src:sqlcc_pch",
    ],
    copts = [
        "-std=c++20",
        "-Iinclude",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = [
        "-pthread",
        "-fprofile-instr-generate",
    ],
    tags = ["coverage", "level4", "sql_processing"],
)

# JSON operations tests
cc_test(
    name = "json_operations_tests",
    srcs = glob([
        "json_operations/*.cpp",
    ], allow_empty = True),
    deps = [
        "//src/sql_parser:json",
        "@com_google_googletest//:gtest_main",
    ],
    copts = [
        "-std=c++20",
        "-Iinclude",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = [
        "-pthread",
        "-fprofile-instr-generate",
    ],
    tags = ["coverage", "level4", "sql_processing"],
)

# Permission validator tests
cc_test(
    name = "permission_validator_tests",
    srcs = glob([
        "permission_validator/*.cpp",
    ], allow_empty = True),
    deps = [
        "@com_google_googletest//:gtest_main",
    ],
    copts = [
        "-std=c++20",
        "-Iinclude",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = [
        "-pthread",
        "-fprofile-instr-generate",
    ],
    tags = ["coverage", "level4", "sql_processing"],
)

# SQL executor core tests
cc_test(
    name = "sql_executor_core_tests",
    srcs = glob([
        "sql_executor_core/*.cpp",
    ], allow_empty = True),
    deps = [
        "//src/storage_engine:storage_engine",
        "@com_google_googletest//:gtest_main",
    ],
    copts = [
        "-std=c++20",
        "-Iinclude",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = [
        "-pthread",
        "-fprofile-instr-generate",
    ],
    tags = ["coverage", "level4", "sql_processing"],
)

# Stored procedure tests
cc_test(
    name = "stored_procedure_tests",
    srcs = glob([
        "stored_procedure/*.cpp",
    ], allow_empty = True),
    deps = [
        "//src/sql_executor:sql_executor",
        "@com_google_googletest//:gtest_main",
    ],
    copts = [
        "-std=c++20",
        "-Iinclude",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = [
        "-pthread",
        "-fprofile-instr-generate",
    ],
    tags = ["coverage", "level4", "sql_processing"],
)

# Trigger tests
cc_test(
    name = "trigger_tests",
    srcs = glob([
        "trigger/*.cpp",
    ], allow_empty = True),
    deps = [
        "//src/trigger:trigger",
        "@com_google_googletest//:gtest_main",
    ],
    copts = [
        "-std=c++20",
        "-Iinclude",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = [
        "-pthread",
        "-fprofile-instr-generate",
    ],
    tags = ["coverage", "level4", "sql_processing"],
)

# Test suite combining all SQL processing tests
test_suite(
    name = "sql_processing_test_suite",
    tests = [
        ":execution_context_tests",
        ":json_operations_tests",
        ":permission_validator_tests",
        ":sql_executor_core_tests",
        ":stored_procedure_tests",
        ":trigger_tests",
    ],
)
