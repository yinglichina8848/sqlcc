load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "network_communication_test",
    srcs = glob(["*.cpp"]),
    deps = [
        "@com_google_googletest//:gtest_main",
        "//src/network:network",
        "//src/storage_engine:storage_engine",
        "//src/core:core",
        "//src/logger:logger",
        "//src/utils:utils",
    ],
    copts = [
        "-std=c++20",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = ["-fprofile-instr-generate"],
    tags = ["coverage", "level6", "integration"],
)