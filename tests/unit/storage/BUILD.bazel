# Storage单元测试BUILD文件

cc_test(
    name = "wal_buffer_test",
    srcs = ["wal_buffer_test.cpp"],
    includes = ["../../../include"],
    deps = [
        "//src/storage_engine:storage_engine",
        "//include/storage:headers",
        "//include/utils:headers",
        "@com_google_googletest//:gtest_main",
    ],
    copts = [
        "-std=c++20",
        "-stdlib=libc++",
    ],
    linkopts = [
        "-stdlib=libc++",
        "-lc++abi",
    ],
)
