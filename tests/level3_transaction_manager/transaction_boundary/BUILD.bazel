load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "transaction_boundary_test",
    srcs = ["transaction_boundary_test.cpp"],
    deps = [
        "@com_google_googletest//:gtest_main",
        "//src/core:core",
        "//src/logger:logger",
        "//src/exception:exception",
        "//src/error_handler:error_handler",
    ],
    copts = [
        "-std=c++20",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = ["-fprofile-instr-generate"],
    tags = ["coverage", "level3", "transaction_manager", "boundary"],
)