cc_test(
    name = "index_manager_tests",
    srcs = glob(["*.cpp"]),
    deps = [
        "//include/storage_engine:index_manager",
        "//include/storage_engine:smart_index_cache",
        "//include/storage_engine:transactional_index_manager",
        "//src/storage_engine:index_manager_lib",
        "//src/storage_engine:smart_index_cache_lib",
        "@com_google_googletest//:gtest_main",
    ],
    copts = [
        "-std=c++20",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = ["-fprofile-instr-generate"],
    tags = ["coverage", "level2", "storage_engine"],
)
