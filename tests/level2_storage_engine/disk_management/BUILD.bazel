cc_test(
    name = "disk_management_tests",
    srcs = glob(["*.cpp"]),
    deps = [
        "//include/storage:disk_manager",
        "//include/storage:disk_error_handler",
        "//src/storage_engine:disk_error_handler_lib",
        "@com_google_googletest//:gtest_main",
    ],
    copts = [
        "-std=c++20",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = ["-fprofile-instr-generate"],
    tags = ["coverage", "level2", "storage_engine"],
)
