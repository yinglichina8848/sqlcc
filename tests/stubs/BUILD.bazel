cc_library(
    name = "network_stub",
    srcs = ["network_stub.cpp"],
    hdrs = [],
    visibility = ["//visibility:public"],
    copts = ["-std=c++20"],
    includes = ["../include"],
    # Test stubs may reference OpenSSL symbols via inline helpers in
    # production headers; link libssl/libcrypto to satisfy those refs.
    linkopts = ["-lssl", "-lcrypto"],
)
