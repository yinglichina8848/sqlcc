"""Bazel构建配置 for SQLCC executor组件测试"""

load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "sql_executor_test",
    srcs = ["sql_executor_test.cpp"],
    includes = ["../../../include", "../../../include/core", "../../../include/executor"],
    deps = [
        "//src/core:sqlcc_core_lib",
        "//src/sql_executor:sqlcc_executor",
        "//src:execution_engine",
        "//src:unified_executor",
        "//src/sql_parser:sqlcc_parser",
        "@com_google_googletest//:gtest_main",
    ],
)

test_suite(
    name = "executor_component_tests",
    tests = [
        ":sql_executor_test",
    ],
)