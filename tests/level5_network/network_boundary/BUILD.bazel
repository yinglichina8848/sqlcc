load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "network_boundary_test",
    srcs = ["network_boundary_test.cpp"],
    deps = [
        "@com_google_googletest//:gtest_main",
        "//src/logger:logger",
        "//src/exception:exception",
    ],
    copts = [
        "-std=c++20",
        "-fprofile-instr-generate",
        "-fcoverage-mapping",
    ],
    linkopts = ["-fprofile-instr-generate"],
    tags = ["coverage", "level5", "network", "boundary"],
)