/**
 * WHY: 为什么数据库系统需要存储访问器接口？
 *
 * 数据库系统是复杂的企业级软件，存储层的设计直接影响系统的可扩展性、可维护性和稳定性：
 * 1. 存储引擎多样性：现代数据库支持多种存储引擎（B+树、LSM树、内存存储等）
 * 2. 事务管理解耦：事务管理器不应直接依赖具体的存储引擎实现
 * 3. 接口标准化：提供统一的存储访问API，便于组件间的协作
 * 4. 依赖倒置：高层模块不依赖底层实现，通过接口隔离变化
 * 5. 测试友好性：接口设计便于mock和单元测试
 *
 * 存储访问器接口的价值体现在：
 * - 提高系统抽象层次：隐藏存储引擎的具体实现细节
 * - 支持存储引擎切换：可以无缝替换不同的存储引擎
 * - 简化系统架构：清晰的层次分离和职责划分
 * - 增强可测试性：基于接口的测试更加可靠和可维护
 * - 促进组件复用：接口标准化促进组件间的复用
 *
 * WHAT: StorageAccessor - 存储访问器接口
 *
 * 定义统一的存储访问契约，提供数据库系统的核心存储操作：
 * - CRUD操作：完整的记录创建、读取、更新、删除功能
 * - 元数据访问：表结构和属性的查询和维护
 * - 表管理：表的生命周期管理和DDL操作
 * - 查询接口：灵活的条件查询和结果返回
 * - 错误处理：标准化的存储操作错误处理
 *
 * 核心特性：
 * - 纯虚接口：定义存储访问的完整契约
 * - 类型安全：强类型的参数和返回值
 * - 异常安全：保证操作的异常安全性
 * - 资源管理：智能指针管理资源生命周期
 * - 扩展性：支持未来存储功能的扩展
 *
 * HOW: 存储访问器接口的设计和技术实现
 *
 * 1. 接口设计原则：
 *    - 单一职责：每个方法只负责一个具体的存储操作
 *    - 依赖倒置：高层模块依赖抽象接口而非具体实现
 *    - 接口隔离：提供最小且完整的接口集合
 *    - 里氏替换：任何实现都可以替代接口使用
 *
 * 2. 方法设计模式：
 *    - 命令模式：每个方法代表一个具体的存储命令
 *    - 模板方法：定义操作的通用流程和步骤
 *    - 工厂方法：创建不同的存储访问器实现
 *
 * 3. 参数和返回值设计：
 *    - 类型安全：使用强类型避免运行时错误
 *    - 资源管理：智能指针管理动态资源
 *    - 错误传播：布尔返回值表示操作成功与否
 *    - 异常安全：保证异常情况下的资源正确释放
 *
 * 4. 扩展性设计：
 *    - 虚析构函数：支持多态删除
 *    - 纯虚函数：强制子类实现所有接口方法
 *    - 默认参数：提供方法调用的灵活性
 *    - 常量引用：避免不必要的拷贝开销
 *
 * 5. 性能优化策略：
 *    - 批量操作：支持批量数据操作减少调用开销
 *    - 延迟加载：按需加载元数据和索引信息
 *    - 连接池：底层存储连接的重用和缓存
 *    - 查询优化：支持查询计划的生成和优化
 *
 * 🏗️ 设计模式：抽象工厂模式 + 桥接模式
 *
 * 抽象工厂模式应用：
 * - StorageAccessor作为抽象产品
 * - StorageAccessorImpl作为具体产品
 * - 通过工厂方法创建不同的存储访问器实例
 *
 * 桥接模式应用：
 * - StorageAccessor接口作为抽象部分
 * - StorageEngine实现作为实现部分
 * - 接口和实现分离，支持独立变化
 *
 * SOLID原则体现：
 *
 * 1. 单一职责原则(SRP)：
 *    - StorageAccessor只定义存储访问的接口契约
 *    - 具体的存储逻辑由实现类负责
 *    - 接口和实现分离，职责清晰
 *
 * 2. 开闭原则(OCP)：
 *    - 接口保持稳定，支持新的实现类扩展
 *    - 通过继承和多态实现功能的扩展
 *    - 不修改现有代码即可添加新的存储引擎
 *
 * 3. 里氏替换原则(LSP)：
 *    - 任何StorageAccessor的实现都可以替代接口使用
 *    - 保证接口契约的正确实现和行为一致性
 *    - 子类可以完全替代父类的使用场景
 *
 * 4. 接口隔离原则(ISP)：
 *    - 提供简洁的存储访问接口
 *    - 避免不必要的接口依赖和方法
 *    - 按需暴露存储功能的各个方面
 *
 * 5. 依赖倒置原则(DIP)：
 *    - 高层模块依赖StorageAccessor抽象接口
 *    - 不依赖具体的StorageEngine实现
 *    - 通过依赖注入提高系统的可测试性
 */

#pragma once

#include <memory>
#include <string>
#include <vector>

namespace sqlcc {

/**
 * WHY: 为什么StorageAccessor需要虚析构函数？
 *
 * C++多态删除要求基类有虚析构函数：
 * - 资源管理：正确释放派生类对象的资源
 * - 内存泄露预防：避免通过基类指针删除派生类对象时的内存泄露
 * - 多态安全性：保证delete操作的类型安全
 * - RAII模式支持：支持资源获取即初始化模式
 *
 * WHAT: StorageAccessor虚析构函数 - 多态删除的安全保证
 *
 * 提供虚析构函数确保多态删除的安全性：
 * - 默认实现：使用default提供默认实现
 * - 虚函数：支持动态绑定和多态删除
 * - noexcept：保证析构不抛出异常
 * - 资源清理：正确清理派生类资源
 *
 * HOW: 虚析构函数的实现机制
 *
 * 使用=default提供默认实现：
 * - 编译器生成：让编译器自动生成析构函数
 * - 性能优化：编译器优化默认实现的性能
 * - 异常安全：保证析构过程中的异常安全性
 * - 资源管理：正确处理成员变量的析构顺序
 */
class StorageAccessor {
public:
    virtual ~StorageAccessor() = default;

    /**
     * WHY: 为什么需要insertRecord方法？
     *
     * 数据插入是数据库系统的核心操作之一：
     * - 数据持久化：将用户数据安全存储到持久化存储中
     * - 完整性保证：确保插入的数据满足数据库约束条件
     * - 并发控制：多用户并发插入时的冲突解决
     * - 索引维护：自动更新相关索引以反映新数据
     * - 事务支持：插入操作的ACID事务语义保证
     *
     * WHAT: insertRecord - 插入新记录到指定表
     *
     * 将一条记录插入到数据库表的操作：
     * - table：目标数据表的名称，标识插入位置
     * - record：要插入的记录数据，向量形式表示字段值
     * - 返回值：布尔值表示插入操作是否成功
     * - 约束检查：自动验证主键、唯一、外键等约束
     * - 索引更新：自动维护表的索引结构
     *
     * HOW: insertRecord接口的设计和使用
     *
     * 1. 参数设计：
     *    - 表名参数：const string& 避免拷贝开销
     *    - 记录参数：const vector<string>& 表示字段值的有序集合
     *    - 字段顺序：向量索引对应表结构中的字段顺序
     *
     * 2. 返回值语义：
     *    - true：记录插入成功，所有约束都满足
     *    - false：插入失败，可能由于约束违反或其他错误
     *    - 异常安全：方法保证不抛出异常，通过返回值报告错误
     *
     * 3. 约束验证：
     *    - 主键约束：检查主键字段的唯一性
     *    - 唯一约束：验证唯一索引字段的唯一性
     *    - 非空约束：确保必填字段都有值
     *    - 外键约束：检查引用完整性
     *    - 检查约束：验证字段值的有效性
     *
     * 4. 性能考虑：
     *    - 批量插入：支持多条记录的批量插入优化
     *    - 索引更新：最小化索引维护的性能开销
     *    - 并发控制：处理高并发场景下的插入冲突
     *    - 缓存失效：更新相关缓存以保持数据一致性
     */
    virtual bool insertRecord(const std::string& table, const std::vector<std::string>& record) = 0;

    /**
     * WHY: 为什么需要updateRecord方法？
     *
     * 数据更新是数据库维护的核心操作：
     * - 数据修正：修改已存在但需要更新的数据
     * - 版本管理：支持数据的版本控制和审计
     * - 增量更新：只更新需要变更的字段
     * - 并发安全：多用户同时更新时的冲突检测和解决
     * - 完整性维护：更新时保持数据的引用完整性
     *
     * WHAT: updateRecord - 更新表中指定记录的数据
     *
     * 根据记录标识符更新数据库表中现有记录的操作：
     * - table：包含要更新记录的表名
     * - record_id：唯一标识要更新记录的ID
     * - record：包含更新后字段值的新记录数据
     * - 返回值：布尔值表示更新操作是否成功
     * - 条件更新：基于记录ID的精确更新操作
     * - 约束验证：确保更新后的数据满足所有约束条件
     *
     * HOW: updateRecord接口的设计和实现考虑
     *
     * 1. 标识符设计：
     *    - 记录ID：使用字符串表示记录的唯一标识符
     *    - 主键字段：通常是表的主键字段值
     *    - 复合键：支持多字段组合的复合主键
     *
     * 2. 更新语义：
     *    - 全记录更新：替换整条记录的所有字段值
     *    - 部分字段更新：只更新指定的字段（接口扩展）
     *    - 条件更新：基于WHERE条件的批量更新（接口扩展）
     *
     * 3. 并发控制：
     *    - 乐观锁：通过版本号或时间戳检测并发冲突
     *    - 悲观锁：通过锁机制防止并发更新
     *    - 冲突解决：定义冲突发生时的解决策略
     *
     * 4. 数据一致性：
     *    - 事务保证：更新操作在事务上下文中执行
     *    - 回滚支持：失败时支持操作的回滚
     *    - 审计记录：记录更新操作的详细信息
     *
     * 5. 性能优化：
     *    - 索引更新：只更新受影响的索引项
     *    - 缓存同步：更新缓存中的对应记录
     *    - 日志优化：最小化WAL日志的写入量
     */
    virtual bool updateRecord(const std::string& table, const std::string& record_id, const std::vector<std::string>& record) = 0;

    /**
     * WHY: 为什么需要deleteRecord方法？
     *
     * 数据删除是数据库维护的重要操作：
     * - 数据清理：移除不再需要的过期或无效数据
     * - 存储回收：释放被删除数据占用的存储空间
     * - 引用维护：正确处理级联删除和外键约束
     * - 审计追踪：记录数据删除操作和原因
     * - 安全删除：防止意外删除重要数据
     *
     * WHAT: deleteRecord - 删除表中指定记录的操作
     *
     * 根据记录标识符删除数据库表中指定记录：
     * - table：包含要删除记录的表名
     * - record_id：唯一标识要删除记录的ID
     * - 返回值：布尔值表示删除操作是否成功
     * - 精确删除：基于记录ID的精确删除操作
     * - 级联处理：自动处理相关外键约束的级联删除
     *
     * HOW: deleteRecord接口的设计和实现要点
     *
     * 1. 删除语义：
     *    - 物理删除：真正从存储中移除记录数据
     *    - 逻辑删除：通过标志位标记记录为已删除（扩展）
     *    - 级联删除：自动删除引用该记录的其他记录
     *
     * 2. 约束处理：
     *    - 外键约束：检查并处理引用完整性约束
     *    - 级联策略：支持不同的级联删除策略
     *    - 约束违反：检测并报告约束违反情况
     *
     * 3. 资源清理：
     *    - 索引更新：从相关索引中移除记录项
     *    - 存储释放：回收记录占用的存储空间
     *    - 缓存清理：从缓存中移除对应的记录
     *
     * 4. 事务保证：
     *    - 原子性：删除操作要么完全成功，要么完全失败
     *    - 隔离性：删除操作对其他事务的影响控制
     *    - 持久性：删除操作的结果要持久保存
     *
     * 5. 安全考虑：
     *    - 权限检查：验证用户是否有删除权限
     *    - 审计记录：记录删除操作的详细信息
     *    - 误删保护：防止意外删除重要数据
     */
    virtual bool deleteRecord(const std::string& table, const std::string& record_id) = 0;

    /**
     * WHY: 为什么需要queryRecords方法？
     *
     * 数据查询是数据库系统最核心和最频繁的操作：
     * - 数据检索：根据条件查找符合要求的数据记录
     * - 业务逻辑：支持各种业务场景的数据查询需求
     * - 性能优化：通过索引和查询计划优化查询性能
     * - 灵活条件：支持复杂的查询条件和逻辑组合
     * - 结果处理：支持结果的排序、分页和聚合
     *
     * WHAT: queryRecords - 查询表中符合条件的记录
     *
     * 根据查询条件从数据库表中检索符合要求的记录集合：
     * - table：要查询的数据表名称
     * - conditions：查询条件字符串（类SQL WHERE子句格式）
     * - 返回值：符合条件的记录列表，每条记录为字段值向量
     * - 查询优化：自动选择最优的查询执行策略
     * - 结果排序：支持结果集的排序和分页
     *
     * HOW: queryRecords接口的设计和实现策略
     *
     * 1. 查询语言：
     *    - 条件语法：支持类SQL的条件表达式语法
     *    - 操作符：支持=, !=, <, >, <=, >=, LIKE, IN等
     *    - 逻辑组合：支持AND, OR, NOT的逻辑运算
     *    - 函数支持：支持常用的SQL函数
     *
     * 2. 查询优化：
     *    - 索引选择：自动选择最合适的索引
     *    - 查询重写：优化查询条件的表达形式
     *    - 执行计划：生成高效的查询执行计划
     *    - 代价估算：基于统计信息估算查询代价
     *
     * 3. 结果处理：
     *    - 分页支持：支持LIMIT和OFFSET子句
     *    - 排序功能：支持ORDER BY子句
     *    - 聚合操作：支持GROUP BY和聚合函数
     *    - 去重处理：支持DISTINCT关键字
     *
     * 4. 性能特征：
     *    - 查询缓存：缓存常用查询的结果
     *    - 并行执行：支持大结果集的并行处理
     *    - 内存管理：控制查询过程中的内存使用
     *    - 超时控制：防止长时间运行的查询
     *
     * 5. 安全考虑：
     *    - SQL注入防护：防止恶意查询条件注入
     *    - 权限验证：检查查询的访问权限
     *    - 资源限制：限制查询的资源使用量
     */
    virtual std::vector<std::vector<std::string>> queryRecords(const std::string& table, const std::string& conditions) = 0;

    /**
     * WHY: 为什么需要getTableMetadata方法？
     *
     * 表元数据是理解和操作数据库表的基础信息：
     * - 结构理解：了解表的字段定义、类型、约束等信息
     * - 动态查询：基于元数据构建动态的查询语句
     * - 数据验证：验证输入数据的格式和类型正确性
     * - 应用集成：为上层应用提供表的结构化信息
     * - 模式演进：支持数据库模式的版本管理和演进
     *
     * WHAT: getTableMetadata - 获取表的元数据信息
     *
     * 获取指定数据表的完整元数据描述信息：
     * - table：要获取元数据的表名
     * - 返回值：表的元数据对象的智能指针
     * - 元数据内容：字段定义、约束、索引、主键、外键等
     * - 缓存策略：支持元数据的缓存以提升访问性能
     * - 权限检查：验证获取元数据的访问权限
     *
     * HOW: getTableMetadata接口的设计和实现
     *
     * 1. 元数据内容：
     *    - 字段信息：字段名、类型、长度、精度等
     *    - 约束信息：主键、唯一、非空、外键约束
     *    - 索引信息：索引名称、字段、类型等
     *    - 表属性：表引擎、字符集、注释等
     *
     * 2. 返回值设计：
     *    - 智能指针：自动管理元数据对象的生命周期
     *    - 空指针：表示表不存在或权限不足
     *    - 异常安全：保证方法调用的异常安全性
     *
     * 3. 缓存机制：
     *    - 内存缓存：缓存常用表的元数据信息
     *    - 缓存策略：LRU或其他缓存替换算法
     *    - 缓存一致性：处理元数据变更时的缓存更新
     *    - 缓存命中率：优化缓存的访问性能
     *
     * 4. 权限控制：
     *    - 访问权限：检查用户是否有查看元数据的权限
     *    - 字段级权限：某些字段可能有特殊的访问限制
     *    - 审计记录：记录元数据的访问操作
     *
     * 5. 性能优化：
     *    - 延迟加载：按需加载完整的元数据信息
     *    - 增量更新：支持元数据的增量更新
     *    - 序列化：支持元数据的序列化和反序列化
     */
    virtual std::shared_ptr<class TableMetadata> getTableMetadata(const std::string& table) = 0;

    /**
     * WHY: 为什么需要tableExists方法？
     *
     * 表的存在性检查是许多数据库操作的前提条件：
     * - 操作验证：确保要操作的表确实存在于数据库中
     * - 错误预防：避免对不存在表的无效操作
     * - 性能优化：快速的存在性检查比实际操作更高效
     * - 用户反馈：提供明确的表不存在错误信息
     * - 并发安全：在高并发环境下的安全存在性检查
     *
     * WHAT: tableExists - 检查指定数据表是否存在
     *
     * 验证数据库中是否存在指定的数据表：
     * - table：要检查的数据表名称
     * - 返回值：布尔值表示表是否存在
     * - 权限无关：只检查存在性，不涉及访问权限验证
     * - 高效查询：适合频繁调用的存在性检查操作
     * - 异常安全：保证检查操作不会抛出异常
     *
     * HOW: tableExists接口的设计和实现要点
     *
     * 1. 检查范围：
     *    - 系统表：检查系统目录表中的表定义
     *    - 用户表：检查用户创建的数据表
     *    - 临时表：检查会话级别的临时表
     *    - 视图对象：某些实现可能包含视图对象
     *
     * 2. 性能考虑：
     *    - 缓存优化：缓存表的元数据信息
     *    - 索引查找：通过索引快速定位表信息
     *    - 批量检查：支持多个表的批量存在性检查
     *
     * 3. 并发处理：
     *    - 读写锁：处理并发访问的读写同步
     *    - 缓存一致性：保证缓存与实际状态的一致性
     *    - 原子操作：确保检查操作的原子性
     *
     * 4. 错误处理：
     *    - 连接问题：处理数据库连接异常情况
     *    - 权限问题：处理可能的权限检查异常
     *    - 系统错误：处理系统级别的检查错误
     *
     * 5. 扩展性：
     *    - 模式支持：支持不同数据库模式的表检查
     *    - 跨库检查：支持跨数据库实例的表存在性检查
     *    - 远程检查：支持分布式环境下的远程表检查
     */
    virtual bool tableExists(const std::string& table) = 0;

    /**
     * WHY: 为什么需要createTable方法？
     *
     * 表创建是数据库模式定义和管理的基础操作：
     * - 模式定义：定义数据的逻辑结构和存储方式
     * - 存储分配：为新表分配必要的物理存储空间
     * - 索引创建：自动创建主键和其他必要的索引结构
     * - 权限设置：为新表设置适当的访问控制权限
     * - 元数据注册：在系统目录中注册表的信息
     *
     * WHAT: createTable - 创建新的数据表
     *
     * 根据提供的结构定义创建新的数据库表：
     * - table：新创建表的名称，必须唯一
     * - schema：表的结构定义字符串（类SQL CREATE TABLE语法）
     * - 返回值：布尔值表示表创建操作是否成功
     * - 完整性保证：确保表结构定义的语法和语义正确性
     * - 权限验证：检查用户是否有创建表的权限
     *
     * HOW: createTable接口的设计和实现策略
     *
     * 1. schema解析：
     *    - 语法解析：解析schema字符串的语法正确性
     *    - 语义验证：检查字段定义、约束等的语义正确性
     *    - 类型检查：验证数据类型的有效性和兼容性
     *
     * 2. 表验证：
     *    - 名称唯一性：确保表名在数据库中唯一
     *    - 名称格式：检查表名的命名规范和限制
     *    - 保留字检查：避免使用数据库保留字作为表名
     *
     * 3. 存储分配：
     *    - 空间预分配：为表数据预分配存储空间
     *    - 文件创建：创建表数据和索引的物理文件
     *    - 初始化设置：设置表的初始存储参数
     *
     * 4. 索引和约束：
     *    - 主键索引：自动创建主键字段的索引
     *    - 唯一约束：创建唯一约束对应的索引
     *    - 外键约束：建立外键约束的引用关系
     *    - 检查约束：验证检查约束的定义正确性
     *
     * 5. 元数据管理：
     *    - 系统目录：将表信息注册到系统目录表
     *    - 统计信息：初始化表的统计信息
     *    - 权限设置：设置表的默认访问权限
     *
     * 6. 事务保证：
     *    - 原子性：表创建要么完全成功，要么完全失败
     *    - 回滚支持：失败时清理所有已创建的资源
     *    - 日志记录：记录表创建操作的WAL日志
     */
    virtual bool createTable(const std::string& table, const std::string& schema) = 0;

    /**
     * WHY: 为什么需要dropTable方法？
     *
     * 表删除是数据库模式管理和维护的重要操作：
     * - 空间回收：释放表及其索引占用的存储空间
     * - 引用清理：正确处理依赖于该表的外键约束
     * - 索引删除：删除表相关的所有索引结构
     * - 元数据清理：从系统目录中移除表的信息
     * - 权限验证：确保用户有足够的权限执行删除操作
     *
     * WHAT: dropTable - 删除指定的数据表
     *
     * 删除数据库中指定的表及其所有相关数据和结构：
     * - table：要删除的表名
     * - 返回值：布尔值表示表删除操作是否成功
     * - 级联删除：自动处理相关的外键约束和引用关系
     * - 数据清理：删除所有数据记录和索引项
     * - 权限检查：验证用户是否有删除表的权限
     *
     * HOW: dropTable接口的设计和实现要点
     *
     * 1. 依赖检查：
     *    - 外键引用：检查是否有其他表引用此表
     *    - 视图依赖：检查是否有视图依赖于此表
     *    - 触发器关联：检查表相关的触发器定义
     *    - 存储过程：检查引用此表的存储过程
     *
     * 2. 删除策略：
     *    - 限制删除：如果存在依赖关系则拒绝删除
     *    - 级联删除：自动删除依赖的对象（可选）
     *    - 设置为空：将外键字段设置为NULL（可选）
     *
     * 3. 资源清理：
     *    - 数据文件：删除表的数据文件和索引文件
     *    - 存储空间：回收占用的磁盘存储空间
     *    - 缓存清理：从各种缓存中移除表相关信息
     *    - 连接清理：关闭相关的数据库连接和游标
     *
     * 4. 元数据更新：
     *    - 系统目录：从系统目录表中移除表记录
     *    - 统计信息：删除表的统计信息记录
     *    - 权限清理：移除表相关的权限设置
     *
     * 5. 事务保证：
     *    - 原子操作：删除操作要么完全成功，要么完全失败
     *    - 回滚能力：支持删除操作的回滚恢复
     *    - 日志记录：记录删除操作的WAL日志
     *
     * 6. 安全考虑：
     *    - 权限验证：严格检查删除权限
     *    - 审计记录：记录表删除操作的详细信息
     *    - 误删保护：防止意外删除重要表
     *    - 备份验证：在删除前验证备份状态
     */
    virtual bool dropTable(const std::string& table) = 0;
};

} // namespace sqlcc
