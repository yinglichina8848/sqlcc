# SQLCC 大规模数据积累性能测试指南

## 概述

本测试套件专门设计用于评估SQLCC服务器在处理大规模数据积累时的性能表现。通过不执行DELETE操作，持续积累数据，可以观察：

- 数据库文件增长情况
- 查询性能随数据量增加的下降趋势
- 服务器在大数据集下的稳定性
- 最优线程池配置的确定

## 测试配置

### 服务器端配置
- **线程池大小**: 8-16个线程
- **推荐配置**: 8线程（基于之前的性能测试结果）

### 客户端配置
- **线程数**: 8-128个线程
- **每线程操作数**: 10,000次CRUD操作
- **操作模式**: INSERT + SELECT + UPDATE（不执行DELETE）

### 测试场景
总共测试 **6种组合**：
- 服务器线程池: [8, 16]
- 客户端线程数: [8, 16, 32]

## 使用方法

### 1. 启动服务器

使用推荐的8线程配置启动服务器：

```bash
bazel run //src/sqlcc_server:server_main -t 8
```

或者使用16线程进行更高负载测试：

```bash
bazel run //src/sqlcc_server:server_main -t 16
```

### 2. 运行数据积累测试

#### 单个测试场景
```bash
# 测试8客户端线程，1万次操作，数据积累模式
bazel run //src/network:true_crud_performance_test -t 8 -o 10000 -a
```

#### 批量测试所有场景
```bash
# 运行完整的大规模测试套件
./run_large_scale_test.sh
```

### 3. 参数说明

#### 服务器参数
- `-t <threads>`: 线程池大小（推荐：8）

#### 客户端参数
- `-h <host>`: 服务器主机（默认：localhost）
- `-p <port>`: 服务器端口（默认：18647）
- `-t <threads>`: 客户端线程数（默认：8）
- `-o <operations>`: 每线程操作数（默认：10000）
- `-a`: 启用数据积累模式（不执行DELETE操作）

## 测试流程

### 数据积累测试流程

1. **初始化阶段**: 建立数据库连接
2. **分阶段测试**:
   - 10%阶段：1,000次操作 × 8线程 = 8,000操作（24,000条记录）
   - 25%阶段：2,500次操作 × 8线程 = 20,000操作（60,000条记录）
   - 50%阶段：5,000次操作 × 8线程 = 40,000操作（120,000条记录）
   - 75%阶段：7,500次操作 × 8线程 = 60,000操作（180,000条记录）
   - 100%阶段：10,000次操作 × 8线程 = 80,000操作（240,000条记录）

3. **性能监控**: 每个阶段记录：
   - 吞吐量（ops/sec）
   - 平均延迟（ms）
   - 成功率（%）
   - 性能下降百分比

## 输出分析

### 性能下降趋势分析

测试会生成详细的性能分析报告：

```
Data Scale vs Performance:
Data Size    Records     Ops/sec   Latency   Success%  Degradation
10%           24,000     850.0     35.2      92.0       0.0%
25%           60,000     780.3     38.4      90.5       8.2%
50%           120,000    695.1     43.1      88.2       18.2%
75%           180,000    621.8     48.2      85.9       26.8%
100%          240,000    589.4     50.8      84.1       30.7%

FINAL ASSESSMENT:
Total data accumulation: 80,000 operations → 240,000 database records
Performance degradation: 30.7%
Final throughput: 589.4 ops/sec
Final latency: 50.8 ms
Assessment: GOOD - Moderate performance impact, acceptable for production
```

### 性能评估标准

- **EXCELLENT** (< 20%): 性能影响很小
- **GOOD** (20-40%): 中等性能影响，可接受
- **FAIR** (40-60%): 显著性能下降，需要优化
- **POOR** (> 60%): 严重性能下降，需要立即处理

## 监控指标

### 数据库文件增长
- 观察数据库文件大小随记录数增加的变化
- 评估存储效率和索引性能

### 查询性能下降
- SELECT操作的响应时间变化
- UPDATE操作的性能影响
- 索引效率随数据增长的衰减

### 服务器稳定性
- 内存使用情况
- CPU利用率
- 连接处理能力

## 最佳实践

### 运行测试的建议

1. **环境准备**:
   - 确保系统有足够的磁盘空间
   - 监控系统资源使用情况
   - 准备好停止测试的应急方案

2. **测试执行**:
   - 从小规模测试开始验证配置
   - 监控服务器日志以发现潜在问题
   - 记录系统资源使用情况

3. **结果分析**:
   - 关注性能下降的拐点
   - 分析不同负载下的行为模式
   - 确定最适合生产环境的配置

### 故障排除

#### 服务器启动失败
```bash
# 检查端口是否被占用
netstat -tlnp | grep 18647

# 强制停止之前的服务器进程
pkill -f "server_main"
```

#### 客户端连接失败
- 确认服务器正在运行
- 检查防火墙设置
- 验证端口配置

#### 性能问题诊断
- 监控系统资源（CPU、内存、磁盘I/O）
- 检查数据库文件大小增长
- 分析服务器日志中的性能指标

## 扩展测试

### 自定义测试参数

可以修改脚本来测试不同的参数组合：

```bash
# 修改run_large_scale_test.sh中的参数
SERVER_THREADS_LIST=(4 8 12 16)  # 添加更多服务器配置
CLIENT_THREADS_LIST=(4 8 16 32 64)  # 扩展客户端配置
OPERATIONS_PER_THREAD=50000  # 增加操作数
```

### 长期稳定性测试

对于更长时间的测试，可以：

1. 增加操作数到50,000或100,000
2. 运行多个测试周期
3. 监控内存泄漏和资源使用趋势

## 结论

这个大规模数据积累测试套件提供了全面的性能评估框架，帮助确定：

- SQLCC在大数据集下的性能特征
- 最优的服务器配置参数
- 系统扩展性和稳定性限制
- 生产环境部署的指导原则

通过系统性的测试和分析，可以确保SQLCC在处理大规模数据应用时能够保持良好的性能和稳定性。