# SQLCC v1.3.8 头文件迁移与BUILD系统重构 TODO清单

## 📋 当前进度概览
- **总体进度**: Phase 1-2 完成，准备Phase 3
- **编译成功率**: 待验证 (重构后重新评估)
- **测试覆盖率**: 待验证 (基础设施重建后评估)

## 🔄 Phase 1: 记录变更 ✅ COMPLETED
**目标**: 完成变更记录和提交

### ✅ 已完成任务
1. **分析git变更**
   - [x] 生成详细的变更统计报告 (61个文件，+1841行，-2690行)
   - [x] 分类整理变更内容 (SQL Parser重构、BUILD系统、执行引擎等)
   - [x] 识别技术成果和影响

2. **准备git提交信息**
   - [x] 撰写详细的提交信息，包含技术变更和成果
   - [x] 成功提交变更到git (提交ID: 4d68132)
   - [x] 更新项目版本状态

## 🔄 Phase 1.5: AST模块Phase-1改进 ✅ COMPLETED
**目标**: 实现AST模块的可遍历、可调试的结构体图

### ✅ 已完成任务
1. **AST基类重构**
   - [x] 更新ast_node.h到sql::ast命名空间
   - [x] 实现统一的accept(NodeVisitor&)接口

2. **NodeVisitor接口实现**
   - [x] 创建node_visitor.h抽象Visitor基类
   - [x] 支持所有AST节点类型访问

3. **AST节点实现**
   - [x] literal_expressions.h - 数值、字符串、布尔、NULL字面量
   - [x] identifier_expression.h - 标识符表达式
   - [x] function_call_expression.h - 函数调用表达式
   - [x] 所有节点实现accept()方法

4. **模块化设计**
   - [x] 创建ast_nodes.h聚合头文件
   - [x] 更新BUILD.bazel配置
   - [x] 实现清晰的include边界

5. **DebugPrintVisitor**
   - [x] 实现debug_printer.h树形结构打印
   - [x] 支持缩进格式化输出
   - [x] 可输出类似"FunctionCall(sum) Identifier(b)"的调试信息

6. **命名空间修复**
   - [x] 修复set_operation.h命名空间问题

### ✅ 验收标准达成
- [x] AST模块成为独立的一等模块 (从Parser副产品升级)
- [x] 完整的Visitor模式实现
- [x] 支持AST遍历和调试输出
- [x] DebugPrinter能够输出正确调试信息

## 🔄 Phase 2: 更新文档 ✅ COMPLETED
**目标**: 更新所有相关文档以反映变更

### ✅ 已完成任务
1. **CHANGELOG.md更新**
   - [x] 添加v1.3.8版本记录
   - [x] 详细描述SQL Parser模块化重构
   - [x] 记录技术成果和质量提升

2. **工作日志和TODO更新**
   - [x] 更新v1.3.8工作日记，记录Phase-1 AST完成情况
   - [x] 更新v1.3.8_TODO.md任务状态
   - [x] 同步项目进度状态

3. **文档同步**
   - [x] README.md版本信息同步
   - [x] RELEASE_NOTES.md准备
   - [x] 文档一致性验证

## 🔄 Phase 2: 业务组件头文件迁移 (进行中)
**目标**: 完成所有业务组件头文件迁移，统一BUILD系统结构

### 📋 当前项目结构分析
- **include目录**: 包含所有业务组件头文件
- **src目录**: 包含各模块源代码，部分已有include子目录
- **需要迁移的模块**: sql_parser, storage, transaction, types, execution, core

### 📋 迁移任务 (更新版)
1. **SQL Parser模块迁移**
   - [ ] 检查 `include/sql_parser/` 目录下所有头文件
   - [ ] 将 `include/sql_parser/` 下所有头文件迁移到 `src/sql_parser/` 目录
   - [ ] 更新 `src/sql_parser/BUILD.bazel` 中的headers目标，设置正确的include路径
   - [ ] 修复所有依赖SQL Parser头文件的模块的include路径
   - [ ] 验证 `//src/sql_parser:sql_parser` 目标可正常编译

2. **Storage模块迁移** ✅ COMPLETED
   - [x] 检查 `include/storage/` 目录下所有头文件
   - [x] 将 `include/storage/` 下所有头文件迁移到 `src/storage/` 目录
   - [x] 更新 `src/storage/BUILD.bazel` 中的headers目标，设置正确的include路径
   - [x] 修复所有依赖Storage头文件的模块的include路径
   - [x] 验证 `//src/storage:storage` 目标可正常编译

3. **Transaction模块迁移** ✅ COMPLETED
   - [x] 检查 `include/transaction/` 目录下所有头文件
   - [x] 将 `include/transaction/` 下所有头文件迁移到 `src/transaction/` 目录
   - [x] 更新 `src/transaction/BUILD.bazel` 中的headers目标，设置正确的include路径
   - [x] 修复所有依赖Transaction头文件的模块的include路径
   - [x] 验证 `//src/transaction:transaction` 目标可正常编译

4. **Types模块迁移** ✅ COMPLETED
   - [x] 检查 `include/types/` 目录下所有头文件
   - [x] 将 `include/types/` 下所有头文件迁移到 `src/types/` 目录
   - [x] 更新 `src/types/BUILD.bazel` 中的headers目标，设置正确的include路径
   - [x] 修复所有依赖Types头文件的模块的include路径
   - [x] 验证 `//src/types:types` 目标可正常编译

5. **Execution模块迁移**
   - [ ] 检查 `include/execution_ast/` 目录下所有头文件
   - [ ] 将 `include/execution_ast/` 下所有头文件迁移到 `src/execution/` 目录
   - [ ] 更新 `src/execution/BUILD.bazel` 中的headers目标，设置正确的include路径
   - [ ] 修复所有依赖Execution头文件的模块的include路径
   - [ ] 验证 `//src/execution:execution` 目标可正常编译

6. **Core模块迁移**
   - [ ] 检查 `include/core/` 目录下所有头文件
   - [ ] 将 `include/core/` 下所有头文件迁移到 `src/core/` 目录
   - [ ] 更新 `src/core/BUILD.bazel` 中的headers目标，设置正确的include路径
   - [ ] 修复所有依赖Core头文件的模块的include路径
   - [ ] 验证 `//src/core:core` 目标可正常编译

7. **其他模块迁移**
   - [ ] 检查 `include/` 目录下其他模块头文件 (database_manager, error_handler, monitoring, network, page, procedure, security, sql_executor, trigger)
   - [ ] 迁移各模块头文件到对应的src/模块名/目录
   - [ ] 更新各模块BUILD.bazel配置
   - [ ] 修复依赖路径
   - [ ] 验证各模块编译

### 📋 迁移原则
1. **就地原则**: 头文件应与实现文件放在同一模块目录下，不创建额外的include子目录
2. **BUILD.bazel**: 每个模块一个cc_library，正确设置includes和hdrs
3. **依赖路径**: 使用基于Bazel target的虚拟根路径，如`#include "sql_parser/parser.h"`
4. **禁止相对路径**: 不使用`#include "../../module/header.h"`形式
5. **禁止系统头风格**: 不使用`#include <module/header.h>`形式

### 📋 迁移后验证
1. **编译验证**: 确保所有模块能正常编译
2. **依赖验证**: 确保所有include路径正确
3. **构建验证**: 确保BUILD系统正确识别头文件
4. **测试验证**: 确保测试用例能正常编译和运行

## 🔄 Phase 3.1: 全核心模块编译打通 (待开始)
**目标**: 将编译成功率从15%提升至70%以上

### 📋 编译验证任务
1. **模块编译验证**
   - [ ] 验证 `//src/sql_parser:lexer` 模块编译
   - [ ] 验证 `//src/sql_parser:parser` 模块编译
   - [ ] 验证 `//src/json:json_lib` 模块编译
   - [ ] 验证 `//src/sql_parser:window_functions` 模块编译
   - [ ] 验证 `//src/sql_executor:executor_lib` 模块编译
   - [ ] 验证 `//src/storage:storage_lib` 模块编译
   - [ ] 验证 `//src/transaction:transaction_lib` 模块编译

2. **C++20兼容性验证**
   - [ ] 确保所有模块在C++20环境下编译通过
   - [ ] 修复编译错误和警告
   - [ ] 验证构建系统稳定性

## 🔄 Phase 3.2: 测试基础设施重建 (待开始)
**目标**: 恢复测试执行能力，建立基础测试覆盖

### 📋 测试恢复任务
1. **Level1基础单元测试**
   - [ ] 检查现有的Level1测试用例和测试框架
   - [ ] 修复测试用例中的include路径问题
   - [ ] 确保基础单元测试能够正常运行
   - [ ] 验证测试覆盖率统计功能

2. **Level2集成测试**
   - [ ] 检查现有的Level2集成测试用例
   - [ ] 修复跨模块依赖的测试问题
   - [ ] 确保集成测试能够正常运行
   - [ ] 验证模块间接口的正确性

3. **存储引擎相关测试**
   - [ ] 检查B+树索引、缓冲池等存储组件的测试用例
   - [ ] 修复存储引擎测试中的路径和依赖问题
   - [ ] 确保存储引擎功能测试能够正常运行
   - [ ] 验证存储组件的正确性和性能

## 🔄 Phase 3.3: 真实功能实现 (架构建立后)
**目标**: 替换模拟功能为真实API集成

### 📋 计划任务
1. **Supabase数据查询替换**
   - [ ] 替换模拟数据为真实数据库查询
   - [ ] 实现连接池管理
   - [ ] 添加错误处理和重试机制

2. **RunPod训练API集成**
   - [ ] 实现真实的模型训练调用
   - [ ] 添加API密钥管理
   - [ ] 实现异步训练任务处理

3. **Hugging Face集成**
   - [ ] 启用预训练模型支持
   - [ ] 实现模型下载和缓存
   - [ ] 添加模型版本管理

## 📊 质量指标监控

### 当前状态
- **编译成功率**: 15% (1/7核心模块)
- **测试覆盖率**: <10% (目标: 62%)
- **构建稳定性**: 需要改进

### 目标指标
- **Phase 3.1结束**: 编译成功率 ≥ 70%
- **Phase 3.2结束**: 测试覆盖率 ≥ 30%
- **Phase 3.3结束**: 功能覆盖率 ≥ 60%

## ⚠️ 风险识别

### 高风险项目
1. **存储引擎编译失败** - 影响核心功能验证
2. **测试基础设施瘫痪** - 无法保证代码质量
3. **循环依赖问题** - 阻碍模块化重构

### 缓解策略
1. **优先级排序**: sql_parser → storage_engine → 测试基础设施
2. **渐进式修复**: 逐模块解决编译问题
3. **质量门禁**: 每个修复后立即验证

## 📅 时间规划

### 本周目标 (1月22-26日)
- [ ] 完成sql_parser所有子模块编译
- [ ] 开始storage_engine模块修复
- [ ] 制定测试基础设施重建计划

### 下周目标 (1月27-2月2日)
- [ ] 完成Phase 3.1构建系统重构
- [ ] 开始Phase 3.2测试基础设施重建
- [ ] 实现第一个真实API集成

---

*最后更新: 2026年1月23日*