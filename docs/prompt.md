整合所有的测试，分为3类：unit，coverage，performance
针对目前的源码，检查并补充代码注释，用 Why，What，How 格式进行说明。
编写核心代码的单元测试和覆盖率测试，要求覆盖90%以上的行，80%以上的分支。
检查编译和测试过程，只能在build目录下生成各种中间文件，重点检查 .o 文件和 .gcov 文件。
每次提交到Gitee之前，必须先编译通过，并且所有的测试都必须通过。
更新变更记录，记录改进，在 ChangeLog 中记录大的改进和变更，重大的错误和缺陷。同时更新其他的MD文档，准备向 Gitee 提交新版本。
先记录改动，然后全面的单元测试，覆盖率测试和性能测试，分析测试的结果：代码覆盖率的情况和性能测试的总结，关键是加锁之后，有没有引起性能的急剧下降。
继续修复失败的单元测试和性能测试以及覆盖率测试中的错误，每修复一个错误，立刻更新ChangeLog VERSION_SUMMARY 然后再继续。不要在主目录下编译和测试！
全部修复后进行单元测试和覆盖率测试分析和性能测试分析，根据测试结果更新所有的文档。
把主目录的测试，都移动到tests下面的unit目录，把开发的简单测试，都加入测试框架，注意不要在主目录生成源码，编译和测试，保持主目录的整洁。
根据进展，更新 docs/项目进展/v1.1.1*  文档中的工作日记和 TODO，然后继续下一步，重点验证核心功能的完整性和真实性。
不要使用 rm ，用DeleteFile工具删除文件。


把所有的测试都集成到测试框架中，整合所有的测试，分为3类：unit，coverage，performance，然后都集成为覆盖率测试。
不要使用 #include “../../../include/xxx.h” ，而是使用 #include “include/xxx.h” 的方式，保持代码的简洁和可维护性。用 -I 参数指定include目录，这样，就可以使用 #include “xxx.h” 的方式引用头文件了。
继续，所有的头文件不在源码目录下，在 项目主目录的 include 目录下，记住这个原则进行查找和修改
不要自行创建头文件，核心代码和头文件都是存在的，只是重新整理了目录结构，进行了移动，认真查找，