整合所有的测试，分为3类：unit，coverage，performance
针对目前的源码，检查并补充代码注释，用 Why，What，How 格式进行说明。
编写核心代码的单元测试和覆盖率测试，要求覆盖90%以上的行，80%以上的分支。
检查编译和测试过程，只能在build目录下生成各种中间文件，重点检查 .o 文件和 .gcov 文件。
每次提交到Gitee之前，必须先编译通过，并且所有的测试都必须通过。
更新变更记录，记录改进，在 ChangeLog 中记录大的改进和变更，重大的错误和缺陷。同时更新其他的MD文档，准备向 Gitee 提交新版本。
先记录改动，然后全面的单元测试，覆盖率测试和性能测试，分析测试的结果：代码覆盖率的情况和性能测试的总结，关键是加锁之后，有没有引起性能的急剧下降。
