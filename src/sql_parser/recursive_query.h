/**
 * WHY: 为什么需要专门的递归查询系统？
 *
 * 数据库系统需要支持复杂的层次结构和递归关系查询，传统方案存在诸多问题：
 * - 层次查询困难：难以表达树状结构和递归关系的查询
 * - 性能瓶颈：递归查询容易导致性能问题和无限循环
 * - 语法复杂：WITH RECURSIVE语法规则复杂难以实现
 * - 语义正确性：递归终止条件和结果正确性难以保证
 * - 扩展性差：难以添加新的递归查询优化策略
 *
 * 递归查询系统的核心价值：
 * 1. 层次数据查询：支持树状结构、图结构等复杂数据的查询
 * 2. 递归关系表达：能够表达祖先-后代、上下级等递归关系
 * 3. 性能优化：通过索引和缓存优化递归查询性能
 * 4. 语义安全：保证递归查询的终止性和结果正确性
 * 5. 标准兼容：完全支持SQL标准中的WITH RECURSIVE语法
 *
 * 🏗️ 设计模式：组合模式(Composite Pattern)
 *
 * 递归查询作为组合模式的经典应用：
 * - 递归结构：递归查询可以包含子查询形成复杂的查询树
 * - 统一接口：所有查询节点实现统一的接口和行为
 * - 树形结构：递归查询形成树形结构，支持嵌套和组合
 * - 动态构建：运行时动态构建递归查询的执行计划
 * - 结果合并：统一的递归查询结果合并和处理机制
 *
 * SOLID原则体现：
 * - 单一职责：递归查询类负责递归关系的查询逻辑
 * - 开闭原则：新递归查询通过扩展现有类实现
 * - 里氏替换：递归查询子类可以替换基类使用
 * - 接口隔离：递归查询接口精确定义所需方法
 * - 依赖倒置：高层模块依赖递归查询接口而非实现
 *
 * WHAT: 递归查询系统 - 完整的层次结构查询框架
 *
 * 核心功能：
 * - 递归CTE定义：支持WITH RECURSIVE语法定义递归表表达式
 * - 基础查询处理：处理递归查询的初始种子数据
 * - 递归查询处理：处理递归查询的迭代逻辑
 * - 终止条件检查：确保递归查询能够正确终止
 * - 结果去重处理：处理递归结果中的重复数据
 * - 性能监控：监控递归查询的执行性能和资源使用
 *
 * 系统组件：
 * - WithRecursiveClause：递归CTE语句的AST节点表示
 * - 基础查询：提供递归查询初始数据的SELECT语句
 * - 递归查询：定义递归关系的SELECT语句
 * - CTE名称：递归公共表表达式的名称标识
 * - 终止检测：检测递归查询何时应该停止的机制
 * - 循环防护：防止无限递归的保护机制
 *
 * 递归查询类型：
 * - 线性递归：每一层的递归只依赖于上一层的结果
 * - 树状递归：每一层的递归可以产生多个子节点
 * - 图遍历：支持图结构的遍历和路径查找
 * - 层次聚合：支持层次结构的聚合计算
 * - 路径跟踪：跟踪递归查询的完整路径信息
 *
 * 递归终止机制：
 * - 空结果终止：当递归查询返回空结果时终止
 * - 深度限制：通过最大递归深度限制防止无限循环
 * - 循环检测：检测并防止递归查询中的循环引用
 * - 条件终止：通过条件判断控制递归查询的终止
 * - 资源限制：通过资源使用限制控制递归查询
 *
 * 接口设计：
 * - CTE创建：根据名称和查询创建递归CTE对象
 * - 查询设置：设置基础查询和递归查询
 * - 选项配置：配置递归查询的各种选项和限制
 * - 执行控制：控制递归查询的执行过程和终止条件
 * - 结果访问：访问递归查询的执行结果和统计信息
 *
 * HOW: 递归查询系统的实现机制
 *
 * 递归查询解析流程：
 * 1. 关键字识别：识别WITH RECURSIVE关键字序列
 * 2. CTE名称解析：解析公共表表达式的名称
 * 3. AS关键字验证：验证AS关键字的存在
 * 4. 括号匹配：验证查询定义的括号匹配
 * 5. 查询解析：递归解析基础查询和递归查询
 * 6. 语义验证：验证递归查询的语义正确性
 * 7. 对象构建：创建递归CTE的AST节点对象
 *
 * 递归查询执行流程：
 * 1. 工作表初始化：创建工作表存储递归中间结果
 * 2. 种子数据加载：执行基础查询加载初始数据
 * 3. 递归迭代：重复执行递归查询直到终止条件满足
 * 4. 结果合并：将每次迭代的结果合并到工作表中
 * 5. 去重处理：根据需要对结果进行去重处理
 * 6. 最终结果：返回完整的递归查询结果
 *
 * 递归终止检测实现：
 * 1. 结果为空检测：检查递归查询是否返回空结果
 * 2. 深度计数器：维护当前递归深度计数
 * 3. 循环检测器：检测结果中是否出现重复数据
 * 4. 条件评估器：评估用户定义的终止条件
 * 5. 资源监控器：监控递归查询的资源使用情况
 *
 * 性能优化策略：
 * - 索引利用：利用索引加速递归查询的基础查询
 * - 中间结果缓存：缓存递归过程中的中间结果
 * - 增量计算：只计算新增的递归结果
 * - 并行执行：对独立的分支进行并行递归计算
 * - 结果压缩：对中间结果进行压缩存储
 *
 * 内存管理策略：
 * - 工作表分区：将大的工作表分区处理
 * - 结果流式处理：对大结果集使用流式处理
 * - 垃圾回收：及时清理不再需要的中间结果
 * - 内存预估：预估递归查询的内存需求
 * - 溢出处理：处理递归查询过程中的内存溢出
 *
 * 循环检测机制：
 * - 哈希签名：为每一行数据生成哈希签名
 * - 签名集合：维护已处理行的签名集合
 * - 重复检测：检测新结果是否与已有结果重复
 * - 循环报告：报告递归查询中的循环情况
 * - 循环终止：根据配置决定是否终止循环查询
 *
 * 语义验证机制：
 * - 列匹配验证：验证基础查询和递归查询的列匹配
 * - 类型兼容检查：检查对应列的数据类型兼容性
 * - 引用完整性：验证递归查询中对CTE的正确引用
 * - 语法正确性：验证WITH RECURSIVE语法的正确性
 * - 逻辑一致性：验证递归逻辑的合理性和一致性
 *
 * 扩展性设计：
 * - 插件架构：支持自定义递归策略和优化算法
 * - 配置化：递归查询行为的配置化管理
 * - 多策略支持：支持不同的递归执行策略
 * - 监控扩展：支持自定义的监控和统计指标
 * - 调试支持：提供递归查询的调试和跟踪功能
 *
 * 调试和诊断：
 * - 执行跟踪：详细记录递归查询的执行轨迹
 * - 性能分析：递归查询的详细性能统计
 * - 结果检查：验证递归查询的中间和最终结果
 * - 循环分析：分析递归查询中的循环和终止情况
 * - 可视化工具：递归查询执行过程的可视化展示
 */

#pragma once

#include "ast/ast_node.h"
#include <memory>
#include <string>

namespace sqlcc {
namespace sql_parser {

class SelectStatement;

/**
 * @brief WITH RECURSIVE语句
 *
 * 表示递归公共表表达式 (Recursive Common Table Expression)
 * 语法: WITH RECURSIVE cte_name AS (base_query UNION recursive_query) SELECT ...
 */
class WithRecursiveClause : public Statement {
public:
    /**
     * @brief 构造函数
     * @param cte_name CTE名称
     * @param base_query 基础查询
     * @param recursive_query 递归查询
     */
    WithRecursiveClause(std::string cte_name,
                       std::unique_ptr<SelectStatement> base_query,
                       std::unique_ptr<SelectStatement> recursive_query);

    /**
     * @brief 析构函数
     */
    ~WithRecursiveClause();

    /**
     * @brief 获取CTE名称
     * @return CTE名称
     */
    const std::string& getCteName() const;

    /**
     * @brief 获取基础查询
     * @return 基础查询指针
     */
    SelectStatement* getBaseQuery() const;

    /**
     * @brief 获取递归查询
     * @return 递归查询指针
     */
    SelectStatement* getRecursiveQuery() const;

    /**
     * @brief 接受访问者
     * @param visitor 访问者对象
     */
    void accept(NodeVisitor& visitor) override;

private:
    std::string cte_name_;                           ///< CTE名称
    std::unique_ptr<SelectStatement> base_query_;    ///< 基础查询
    std::unique_ptr<SelectStatement> recursive_query_; ///< 递归查询
};

} // namespace sql_parser
} // namespace sqlcc
