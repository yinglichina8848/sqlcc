#ifndef STATEMENT_H
#define STATEMENT_H

// 包含include目录下的官方头文件，避免类重定义
#include "../include/sql_parser/ast_node.h"
#include "../include/sql_parser/ast_nodes.h"

// 这里不再重复定义Statement类及其子类，统一使用include目录下的定义

#endif // STATEMENT_H