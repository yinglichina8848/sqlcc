package(default_visibility = ["//visibility:public"])

cc_library(
    name = "utils",
    srcs = glob(["*.cpp"]),
    hdrs = glob(["*.h"]),
    strip_include_prefix = "",
    include_prefix = "",
    deps = [
        # utils 层级最低，无需依赖其他模块
    ],
    visibility = ["//visibility:public"],
)
