# src/utils/BUILD.bazel - 基础设施工具库（严格规范化）

load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "utils",
    srcs = glob(["*.cpp"]),
    hdrs = glob(["*.h"]),
    # strip_include_prefix = "src/utils",
    # utils 作为基础层级，无依赖
    visibility = ["//src:__subpackages__"],
)
