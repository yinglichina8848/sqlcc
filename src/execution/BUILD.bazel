package(default_visibility = ["//visibility:public"])

# -----------------------------------------------------------------------------
# 源文件库 - execution 模块的核心实现
# -----------------------------------------------------------------------------

cc_library(
    name = "execution",
    srcs = glob(["*.cpp"]),
    hdrs = glob(["*.h"]),
    strip_include_prefix = "",
    include_prefix = "",
    copts = [
        "-I.",
    ],
    deps = [
        "//src/utils:utils",
        "//src/storage_engine:storage_engine",
        "//src/sql_parser:sql_parser",
        "//src/execution_ast:execution_ast",
        "//src/types:types",
        "//src/procedure:procedure",
        "//src/trigger:trigger",
        "//src/database_manager:database_manager",
        "//src/exception:exception",
        "//src/logger:logger",
        "//src:wal_manager",
        "//src:error_handler",
        "//src/core:headers",
        "//backups/core_backup_20260121_001034:headers",
    ],
    visibility = ["//src:__subpackages__"],
)

# -----------------------------------------------------------------------------
# 头文件库 - 向后兼容的头文件导出 (已迁移)
# -----------------------------------------------------------------------------

cc_library(
    name = "headers",
    hdrs = glob(["*.h"]),
    visibility = ["//visibility:public"],
)