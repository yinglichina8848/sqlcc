/**
 * WHY: 为什么需要专门的客户端连接管理？
 *
 * 数据库客户端与服务器之间的通信建立在可靠的TCP连接之上，传统方案存在诸多技术挑战：
 * - 连接建立和断开的管理复杂：网络异常、超时、重连等场景处理困难
 * - TLS加密握手流程繁琐：证书验证、密钥协商、安全参数配置等步骤复杂
 * - 连接状态跟踪不准确：连接的有效性检测和状态维护问题
 * - 资源泄漏和异常处理：文件描述符、SSL上下文等资源的释放问题
 * - 并发安全保证缺失：多线程环境下的连接操作线程安全问题
 * - 连接性能优化不足：连接复用、缓冲区管理等性能优化缺失
 * - 错误诊断和监控困难：连接问题的排查和监控手段不足
 *
 * 客户端连接管理系统的核心价值：
 * 1. 连接生命周期全管理：提供完整的连接建立、维护和断开管理
 * 2. TLS安全通信保证：实现完整的TLS握手和安全通信协议
 * 3. 异常情况优雅处理：网络异常、超时等情况的优雅处理和恢复
 * 4. 资源管理自动化：文件描述符、SSL上下文等资源的自动管理
 * 5. 性能优化和监控：连接性能优化和实时状态监控
 * 6. 线程安全并发访问：多线程环境下的连接操作安全保证
 * 7. 诊断和故障排查：详细的连接日志和错误诊断信息
 *
 * 🏗️ 设计模式：RAII模式(Resource Acquisition Is Initialization) + 状态模式(State Pattern)
 *
 * 客户端连接作为RAII模式的应用：
 * - 资源获取与初始化：在构造函数中完成所有资源分配
 * - 异常安全保证：通过析构函数确保资源在异常情况下正确释放
 * - 作用域生命周期：资源生命周期与对象生命周期严格绑定
 * - 自动化管理：无需手动管理资源释放，防止遗漏和泄漏
 * - 编译期检查：编译器保证资源管理语义的正确性
 * - 异常传播安全：异常情况下仍保证资源正确释放
 *
 * SOLID原则体现：
 * - 单一职责：专门负责客户端连接的建立和管理
 * - 开闭原则：新连接类型通过继承现有类实现
 * - 里氏替换：所有连接实现都可以互相替换
 * - 接口隔离：连接接口精确定义连接管理契约
 * - 依赖倒置：依赖抽象的文件描述符和SSL接口
 *
 * WHAT: 客户端连接管理系统 - 安全可靠的网络通信基础设施
 *
 * 核心功能：
 * - TCP连接建立管理：与服务器建立可靠的TCP连接
 * - TLS加密通信支持：完整的TLS握手和加密通信
 * - 连接状态监控：实时监控连接的有效性和状态
 * - 异常处理和恢复：网络异常情况的处理和自动恢复
 * - 资源生命周期管理：文件描述符、SSL上下文等资源的完整管理
 * - 性能优化配置：连接缓冲区、超时等参数的性能优化
 * - 安全配置管理：证书验证、加密算法等安全参数配置
 *
 * 系统组件：
 * - ClientConnection：核心连接类，提供完整的连接管理功能
 * - SSLWrapper：SSL封装器，处理TLS加密和证书验证
 * - FileDescriptor：文件描述符RAII包装器，安全管理socket资源
 * - ConnectionMonitor：连接监控器，监控连接状态和性能
 * - ErrorHandler：错误处理器，处理连接相关的异常情况
 * - ConfigManager：配置管理器，管理连接相关的配置参数
 * - Logger：日志记录器，记录连接操作的详细日志
 *
 * TCP连接管理：
 * - 连接建立：解析主机地址、创建socket、发起连接请求
 * - 连接验证：检查连接的有效性和可达性
 * - 连接维护：定期检查连接状态，处理保活和心跳
 * - 连接断开：优雅关闭连接，释放所有相关资源
 * - 连接重试：连接失败时的自动重试机制
 * - 连接池化：支持连接复用提高性能
 *
 * TLS加密通信：
 * - 证书验证：验证服务器证书的合法性和可信性
 * - 密钥协商：进行TLS密钥交换和协商
 * - 加密传输：对所有数据进行加密传输
 * - 完整性保护：使用HMAC保证数据完整性
 * - 握手优化：TLS会话复用和握手优化
 * - 证书更新：支持证书的动态更新和轮换
 *
 * 连接状态管理：
 * - 连接状态跟踪：准确跟踪连接的当前状态
 * - 状态转换验证：验证状态转换的合法性和安全性
 * - 状态变更通知：通知相关组件连接状态的变化
 * - 状态持久化：重要状态的持久化存储
 * - 状态恢复机制：系统重启后的状态恢复
 * - 状态监控统计：连接状态的统计和监控
 *
 * 异常处理机制：
 * - 连接超时处理：连接建立和数据传输的超时控制
 * - 网络异常恢复：网络故障后的自动重连和恢复
 * - SSL错误处理：TLS握手和加密过程中的错误处理
 * - 资源耗尽处理：系统资源不足情况的处理
 * - 并发访问控制：多线程访问的同步和互斥控制
 * - 错误日志记录：详细的错误信息记录和诊断
 *
 * 资源管理策略：
 * - 文件描述符管理：使用RAII保证socket正确关闭
 * - SSL上下文管理：自动管理SSL_CTX和SSL对象的生命周期
 * - 内存缓冲区管理：高效的数据发送和接收缓冲区管理
 * - 连接配置管理：连接参数的动态配置和管理
 * - 资源使用监控：连接资源的实时使用情况监控
 * - 资源泄漏检测：自动检测和防止资源泄漏问题
 *
 * 接口设计：
 * - 连接建立接口：建立和管理TCP连接的接口
 * - TLS配置接口：配置和管理TLS加密的接口
 * - 数据传输接口：发送和接收数据的接口
 * - 状态查询接口：查询连接状态的接口
 * - 配置管理接口：管理连接配置参数的接口
 * - 监控统计接口：监控连接性能的接口
 * - 错误处理接口：处理连接异常的接口
 *
 * HOW: 客户端连接管理系统的实现机制
 *
 * RAII模式实现：
 * 1. 构造函数资源分配：在构造函数中完成所有资源的分配和初始化
 * 2. 析构函数资源释放：在析构函数中保证所有资源正确释放
 * 3. 异常安全保证：即使发生异常也保证资源正确释放
 * 4. 移动语义支持：支持对象移动而非拷贝的资源管理
 * 5. 资源所有权明确：明确资源的拥有者和责任人
 * 6. 生命周期绑定：资源生命周期与对象生命周期严格绑定
 *
 * 状态模式支撑：
 * 1. 状态接口定义：定义连接状态的通用接口和行为
 * 2. 具体状态实现：未连接、连接中、已连接、TLS握手中等状态
 * 3. 状态转换逻辑：定义状态间的转换规则和触发条件
 * 4. 状态行为委托：不同状态下的行为委托给对应状态处理器
 * 5. 状态扩展机制：支持新连接状态的动态添加
 * 6. 状态验证逻辑：状态转换的合法性验证和错误处理
 *
 * TCP连接实现：
 * 1. 地址解析机制：将主机名解析为IP地址
 * 2. Socket创建过程：创建TCP socket并设置基本参数
 * 3. 连接建立流程：发起connect调用并处理异步连接
 * 4. 连接验证机制：通过发送测试数据验证连接有效性
 * 5. 连接参数配置：设置TCP缓冲区、超时等参数
 * 6. 非阻塞I/O支持：支持非阻塞连接建立和数据传输
 *
 * TLS握手实现：
 * 1. SSL上下文初始化：创建和配置SSL_CTX对象
 * 2. 证书链加载：加载CA证书和客户端证书链
 * 3. 握手协议执行：执行完整的TLS握手协议
 * 4. 密钥协商过程：进行密钥交换和协商
 * 5. 证书验证流程：验证服务器证书的合法性
 * 6. 会话参数配置：配置加密算法和安全参数
 *
 * 数据传输实现：
 * 1. 发送缓冲区管理：管理待发送数据的缓冲区
 * 2. 接收缓冲区管理：管理接收数据的缓冲区
 * 3. 数据分片处理：大块数据的分片发送和重组
 * 4. 流量控制机制：防止发送缓冲区溢出
 * 5. 超时处理逻辑：数据传输的超时控制和重传
 * 6. 错误恢复机制：传输错误的自动重试和恢复
 *
 * 异常处理实现：
 * 1. 连接超时检测：检测连接建立和数据传输超时
 * 2. 网络错误分类：对不同网络错误的分类处理
 * 3. SSL错误处理：TLS相关的错误特殊处理
 * 4. 自动重连机制：连接断开后的自动重连
 * 5. 指数退避策略：重连失败时的指数退避重试
 * 6. 错误日志记录：详细的错误信息记录和分析
 *
 * 资源管理实现：
 * 1. 文件描述符RAII：使用FileDescriptor类管理socket
 * 2. SSL对象RAII：使用SSLSocket类管理SSL连接
 * 3. 内存缓冲区池：复用内存缓冲区减少分配开销
 * 4. 配置对象管理：连接配置参数的动态管理
 * 5. 引用计数机制：智能指针的自动引用计数
 * 6. 垃圾回收机制：不再使用的资源自动回收
 *
 * 并发控制实现：
 * 1. 线程安全设计：所有公共接口都是线程安全的
 * 2. 互斥锁保护：使用互斥锁保护共享状态
 * 3. 原子操作应用：状态标志的原子操作更新
 * 4. 条件变量同步：线程间的状态变更通知
 * 5. 读写锁优化：读多写少的场景性能优化
 * 6. 锁竞争监控：锁竞争情况的监控和调优
 *
 * 扩展性设计：
 * - 插件架构：支持自定义认证和加密插件
 * - 多协议支持：支持不同网络协议的扩展
 * - 云原生适配：支持云环境下的弹性连接管理
 * - 监控集成：支持第三方监控系统的集成
 * - 安全增强：支持零信任安全模型的集成
 *
 * 调试和诊断：
 * - 连接日志追踪：详细记录连接建立和断开过程
 * - TLS握手调试：TLS握手的详细调试信息
 * - 性能监控分析：连接性能指标的实时监控
 * - 错误堆栈追踪：异常情况下的完整堆栈信息
 * - 状态图可视化：连接状态转换的可视化展示
 * - 诊断工具集成：支持网络诊断工具的集成
 */

#pragma once

#include <string>
#include <vector>
#include <memory>
#include "utils/ssl_wrapper.h"
#include "utils/file_descriptor.h"

namespace sqlcc {
namespace network {

/**
 * @brief 客户端连接类，管理TCP连接和TLS握手
 *
 * ClientConnection类负责建立和管理与服务器的TCP连接，
 * 支持TLS加密通信，包括证书验证和安全握手。
 */
class ClientConnection {
public:
    /**
     * @brief 构造函数
     * @param host 服务器主机地址
     * @param port 服务器端口号
     */
    ClientConnection(const std::string& host, int port);

    /**
     * @brief 析构函数，确保连接正确关闭
     */
    ~ClientConnection();

    /**
     * @brief 启用TLS加密
     * @param enabled 是否启用TLS
     */
    void EnableTLS(bool enabled);

    /**
     * @brief 配置TLS客户端证书验证
     * @param ca_cert_path CA证书文件路径
     * @return 配置是否成功
     */
#ifdef __linux__
    bool ConfigureTLSClient(const std::string& ca_cert_path);
#endif

    /**
     * @brief 建立连接
     * @return 连接是否成功
     */
    bool Connect();

    /**
     * @brief 断开连接
     */
    void Disconnect();

    /**
     * @brief 检查连接状态
     * @return true表示已连接，false表示未连接
     */
    bool IsConnected() const;

    /**
     * @brief 发送数据
     * @param data 要发送的数据
     * @return 发送是否成功
     */
    bool SendData(const std::vector<char>& data);

    /**
     * @brief 接收数据
     * @return 接收到的数据，如果失败返回空向量
     */
    std::vector<char> ReceiveData();

private:
    std::string host_;                    ///< 服务器主机地址
    int port_;                           ///< 服务器端口号
    bool connected_;                     ///< 连接状态
    bool tls_enabled_;                   ///< TLS启用状态
    std::string ca_cert_path_;           ///< CA证书路径

    // Linux specific members
#ifdef __linux__
    sqlcc::FileDescriptor socket_fd_;  ///< Socket文件描述符
    sqlcc::utils::SSLSocket ssl_;             ///< SSL连接对象
    sqlcc::utils::SSLContext ssl_ctx_;        ///< SSL上下文对象
#endif
};

} // namespace network
} // namespace sqlcc
